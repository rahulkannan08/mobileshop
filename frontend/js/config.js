// Central API base for frontend. Change if backend runs on a different host/port.
const API_BASE = (function () {
  // default to localhost:5000 for your local backend
  return 'http://localhost:5000';
})();